# Artifacts — Requirements (evidence / history)

> **Artifact truth rule.** Files under `.claude-framework/artifacts/` are working
> records / evidence / history — **NOT current truth**. The curated Context Layer
> (`.claude-framework/adapter/`) is current truth; a working artifact becomes
> durable truth only after promotion into curated context via the human gate
> (Context Promotion).

## What lives here

| File | Role |
|---|---|
| `MOBILE_RUNNER_MASTER_REQ.md` | **Evidence mirror** of the canonical Master REQ. Convenience copy only. |

## Where the canonical requirement lives

The **canonical** Master Requirements Specification for this repository is:

- [`docs/requirements/MOBILE_RUNNER_MASTER_REQ.html`](../../../docs/requirements/MOBILE_RUNNER_MASTER_REQ.html) — the authoritative source.
- [`docs/requirements/MOBILE_RUNNER_MASTER_REQ.md`](../../../docs/requirements/MOBILE_RUNNER_MASTER_REQ.md) — its faithful Markdown conversion.

If the mirror in this directory ever diverges from the canonical `docs/` copies,
**the canonical `docs/` copies win**. Re-sync the mirror from `docs/`; never edit
product requirements here.
