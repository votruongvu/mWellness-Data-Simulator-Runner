# mWellness-Mobile-Runner — MR-B User Stories v2 Backend URLs

Target import path:

```text
artifacts/stories/mr-b/
```

Flatten all `stories/STORY-*.md` files into `artifacts/stories/mr-b/`.

Keep `USER_STORY_INDEX.md` as canonical execution order.

MR-B is the second low-overhead loop:
Runnable Data Loading + Dry-run Planner.

This v2 package includes the concrete backend route URLs from the MWDS router.
