# mWellness-Mobile-Runner — MR-DESIGN-00 User Stories

Target import path:

```text
artifacts/stories/mr-design-00/
```

Flatten all `stories/STORY-*.md` files into `artifacts/stories/mr-design-00/`.

Keep `USER_STORY_INDEX.md` as canonical execution order.

This phase normalizes the refined Claude Designer package into implementation-ready design artifacts. It is design/docs-only.
