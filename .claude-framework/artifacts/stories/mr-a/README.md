# mWellness-Mobile-Runner — MR-A User Stories

Target import path:

```text
artifacts/stories/mr-a/
```

Flatten all `stories/STORY-*.md` files into `artifacts/stories/mr-a/`.

Keep `USER_STORY_INDEX.md` as canonical execution order.

MR-A replaces the old MR0 + MR1 split with one lower-overhead loop:
Foundation + Contracts + Auth Shell.
