# mWellness-Mobile-Runner — MR-FRAMEWORK-00 User Stories

Target import path:

```text
artifacts/stories/mr-framework-00/
```

Flatten all `stories/STORY-*.md` files into `artifacts/stories/mr-framework-00/`.

Keep `USER_STORY_INDEX.md` as canonical execution order.

This phase bootstraps the dedicated Mobile Runner Claude Framework. It is framework/docs-only.
