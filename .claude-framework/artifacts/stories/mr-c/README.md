# mWellness-Mobile-Runner — MR-C User Stories

Target import path:

```text
artifacts/stories/mr-c/
```

Flatten all `stories/STORY-*.md` files into `artifacts/stories/mr-c/`.

Keep `USER_STORY_INDEX.md` as canonical execution order.

MR-C is the native writer MVP loop:
iOS HealthKit + Android Health Connect, with strict payload and real-write gates.
